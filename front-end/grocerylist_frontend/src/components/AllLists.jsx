export default function AllLists ({type}) {

    if (type === "home") {
        return (
            <div>
               
            </div>

             //logica de aparecer no máximo 3 listas (cards)
        )
    }

    if (type === "all") {
        return (

            <div>
               
            </div>
            
             //logica de aparecer todas as listas (cards) 
            
        )
    }

    return (
        <div></div>
    )
}